<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />

		<title>{{ title }}</title>

		<link rel="stylesheet" href="{{ style.font.link }}" />
		<style>
			html {
				/*
					Put serif as fallback for sans-serif so it's apparent if the font
					doesn't get applied.
				*/
				font-family: "{{ style.font.name }}", serif;
			}
			h1 {
				font-size: {{ style.font.sizes.mainHeading }}px;
			}
			h2 {
				font-size: {{ style.font.sizes.subHeadings }}px;
			}
			h1,
			h2 {
				color: {{ style.colors.headings }};
			}
			p {
				font-size: {{ style.font.sizes.paragraphs }}px;
				color: {{ style.colors.paragraphs }};
			}
			a {
				color: {{ style.colors.links }};
			}
			img {
				display: block;
				margin-top: 45px;
			}
		</style>
	</head>
	<body>
		<h1>{{ title }}</h1>
		<p>{{ subtitle }}</p>

		<h2>
			Photo by
			<a href="{{ image.photographer.link }}">{{ image.photographer.name }}</a>
			on
			<a href="{{ image.from.link }}">{{ image.from.name }}</a>
		</h2>
		<img src="{{ image.link }}" alt="{{ image.alt }}" />
	</body>
</html>
